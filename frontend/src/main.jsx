import React from 'react';
import ReactDOM from 'react-dom/client';  // Note the correct path
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
